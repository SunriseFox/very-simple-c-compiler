int func() {
  int i = 9 + 9;
  return i;
}

int main() {
    int i, result = 1;
    scanf("%d", &i);

    while (i > 0) {
        result *= i;
				i = i - 1;
    }

    int sth = func();
    printf("%d, %d\n", result, sth);
    return 0;
}
